/** Multi Line Comments Go here
and here
and here
*/

class Utility
{
    // This Line is to be ignored by the Scanner

    float var2 ;

    int findMax(int array[100])
    {
        int maxValue ;
        for( int idx = 99; idx > 0; idx = idx - 1 )
        {

         // statement body goes here

        };
    return (maxValue);
    };



};

program {

    int minValue;
    // Utility utility;

    maxValue = utility.findMax(sample);
    // put(maxValue);
};